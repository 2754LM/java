    # cookie = login_with_cookies()
    # all_job_data = get_boss_data(cookie)
    # with open('job_data.json', 'w', encoding='utf-8') as f:
    #     json.dump(all_job_data, f, ensure_ascii=False, indent=2)
    # print(f"总共爬取了 {len(all_job_data)} 条数据，已保存到 job_data.json")